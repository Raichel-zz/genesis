<%= @page_title = 'Edit Theme' %>
<nav>
  <p><%= link_to("<< Back to List", {:action => "list"}, :class => "back-link")%></p>
</nav>

<div>
  	<%= form_for(:theme, :url => {:action => 'update', :id => @theme.id}) do |t| %>
      	<table>
      		<tr>
      			<th>Name</th>
      			<td><%= t.text_field(:name) %></td>
      		</tr>
      		<tr>
      			<th>File URL</th>
      			<td><%= t.text_field(:file_url) %></td>
      		</tr>
      	</table>
      	
      	<div class="form-buttons">
      		<%= submit_tag("Update Theme")%>
      	</div>
  	<% end %>
</div>