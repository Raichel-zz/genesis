<%= @page_title = 'Delete confirmation' %>
<nav>
  <p><%= link_to("<< Back to List", {:action => "list"}, :class => "back-link")%></p>
</nav>

<div>
  	<%= form_for(:background, :url => {action: 'destroy', id: @background.id}) do |t| %>
      	
		<p>Are you sure you want to delete this Background?</p>
      	
      	<p><%= @background.name%></p>
      	
      	<div class="form-buttons">
      		<%= submit_tag("Delete Background")%>
      	</div>
  	<% end %>
</div>
